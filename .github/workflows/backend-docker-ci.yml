name: Backend Docker CI

on:
  push:
    paths:
      - 'backend/**'
      - '.github/workflows/backend-docker-ci.yml'
  pull_request:
    paths:
      - 'backend/**'

jobs:
  docker-build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        working-directory: backend
        run: docker build -t backend-ci-test .

      - name: Run container
        run: |
          docker run -d -p 5001:5000 --name backend-ci backend-ci-test
          sleep 10

      - name: Health check
        run: curl -f http://localhost:5001/health

      - name: Stop container
        if: always()
        run: docker stop backend-ci && docker rm backend-ci
